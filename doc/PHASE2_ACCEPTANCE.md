# 阶段二验收记录

日期：2026-08-03

## 已完成

- 模板插入和复制会自动识别声明变量及 Prompt 中的 `{variable}` 占位符。
- 带变量的模板先打开“填写任务信息”表单。
- 表单支持中英文标签、必填校验和多行文本输入。
- 表单内实时显示变量替换后的 Prompt 预览。
- 支持“生成并插入”和“生成并复制”。
- 没有变量的模板继续直接插入或复制，兼容原有流程。
- 未填写完整时不会执行插入或复制。

## 自动验证

- `npx tsc --noEmit`：通过。
- `npx vite build --outDir .phase2-dist`：通过。
- 已删除临时构建目录 `.phase2-dist`。
- 当前模板数据中有 66 个模板声明了变量，均可进入变量表单流程。

## 手工验收清单

在 Chrome 重新加载扩展后：

- [ ] 打开一个带变量的模板，例如“文章大纲”，点击“插入”。
- [ ] 填写主题后，预览中的 `{topic}` 已被替换。
- [ ] 点击“生成并插入”，在 ChatGPT 或 Claude 输入框中看到替换后的 Prompt。
- [ ] 点击“生成并复制”，在非 AI 页面也能复制完整 Prompt。
- [ ] 留空一个必填字段，确认不会执行插入或复制并显示提示。
- [ ] 打开一个不带变量的模板，确认仍然可以直接插入/复制。
- [ ] 切换中文和英文，确认表单标题、字段标签、按钮文案和预览正常。
- [ ] 阶段一的优化预览、撤销和额度流程没有回归。

阶段三在以上手工清单确认前不启动。
